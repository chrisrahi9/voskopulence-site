import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Sustainability & Formulation",
  description:
    "How Voskopulence approaches naturally derived ingredients, solid formats, packaging and COSMOS-style formulation principles.",
};

export default function SustainabilityLayout({ children }: { children: React.ReactNode }) {
  return children;
}
